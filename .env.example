# Server-side only (read by api/parse-recipe.js and the Vite dev server).
# Do NOT prefix with VITE_ — VITE_ variables are embedded in the public browser bundle.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=qwen/qwen3.5-35b-a3b
